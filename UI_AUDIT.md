# UI_AUDIT.md — dsh-usage-dashboard 界面审计报告

> 审计日期：2026-08-15 ｜ 审计对象：设置页「Token 用量」仪表盘（`src/client/index.tsx`）
> 审计方法：源码审读 + headless Chromium（1440×900 / 768×1024 / 430×900 三种视口）实测 DOM 几何 + 截图复核

## 1. 运行环境基线（实测）

| 视口 | 设置对话框宽 | 仪表盘内容区宽（udb-root） | 4 列时单卡宽 |
|---|---|---|---|
| 1440×900 | 800px | **556px** | 131.5px |
| 1024×768 | 800px | **556px** | 131.5px |
| 768×1024 | 720px | **476px** | 111.5px |
| 430×900 | 382px | ~138px | 64px |

**结论**：该仪表盘永远渲染在设置对话框内，内容区最宽仅 556px。原 `repeat(4, 1fr)` 的指标卡栅格在此宽度下每卡仅 111–131px，是布局问题的根源。

## 2. 问题清单（按严重度排序）

### P0 — 数据可读性受损
1. **指标卡数值全部溢出截断**：实测 4 张卡的 `scrollWidth > clientWidth` 全部为 `true`，如总消耗「608,186,980」在 20px 字号下需要 111px，而卡内可用宽度仅 85px → 显示为「608,186,…」。截图（before-1440.png）确认截断。
2. **指标卡副标题同样截断**：如「1.2% · 缓存写入 0」「…推理 0」被 `text-overflow: ellipsis` 截掉（截图确认「缓存写…」）。

### P1 — 布局与栅格
3. **4 列栅格与容器不匹配**：556px 内容区放 4 列卡片过挤；窄屏（≤640px）虽降为 2 列，但 430px 视口下 1 列卡片仅 64px 宽，仍不可用。
4. **会话列表序号不对齐**：序号以 `(i+1) + '. ' + title` 拼进同一文本，1–9 与 10+ 前缀宽度不同，各行的标题不在同一列上。
5. **模型分布彩虹色**：palette 7 种不同色相（蓝/绿/琥珀/信息蓝/红/灰/半透明蓝），视觉噪声大，且与图表语义色（输入=蓝、输出=绿、缓存=琥珀）混淆——分布条颜色不代表任何语义。

### P2 — 视觉与一致性
6. **「总消耗」与「输入」色点同为品牌蓝**：两张卡色点完全同色，无法区分主次指标。
7. **间距系统不统一**：根间距 16px / 卡片间距 10px / 卡片内边距 12×14 vs 14×16 / 卡片标题下边距 10px，没有统一刻度。
8. **圆角不统一**：指标卡 14px、卡片 14px、按钮 8px、分段控件 8px、进度条 16px 圆角（进度条圆角过大）。
9. **对比度偏低**：大量 11px 辅助文本使用 `label-tertiary`（约 3.2:1，低于 WCAG AA 4.5:1）；副标题、卡片备注、页脚应至少用 `label-secondary`。
10. **缺少交互态**：按钮/分段控件/链接均无 `:focus-visible` 焦点环；分段控件无 hover 反馈；扫描徽标无活动指示。

### P3 — 细节
11. 空状态仅为纯文本，无视觉容器区分；扫描中无动效反馈。
12. 卡片标题无层级引导，四张图卡标题与正文区分度弱。
13. 进度条轨道 16px 过粗，与 12px 行高不协调。
14. 无 `prefers-reduced-motion` 适配（本次新增动效后需要）。

## 3. 保留不动（合规边界）
- 数据获取：`GET /usage/api/dashboard` 轮询（4s）、`hours`/`top` 参数、`reloadTick` 手动刷新 —— **不改**。
- 组件 API / props 签名、`settings.section` 注册（order 40）、i18n 字典、SVG 图表数据计算（niceMax/fmtCompact/堆叠逻辑）—— **不改**。
- 宿主半 `src/index.ts`、构建配置 —— **不改**。

## 4. 复现截图
- `before-1440.png`（/tmp/udb-cdp/）：1440×900 下指标卡数值截断、4 列过挤。
