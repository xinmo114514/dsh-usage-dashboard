# UI_OPTIMIZATION_PLAN.md — 优化方案

> 依据 `UI_AUDIT.md` 的问题清单制定。原则：**小步快跑、不碰数据逻辑、不改组件对外 API**；样式沿用 shell 的 `--dsw-alias-*` 设计令牌，保证明暗主题自动适配。

## 1. 设计基线（默认假设，均可用令牌覆盖）
- 无品牌规范 → 以 shell 现有 `--dsw-alias-brand-primary`（中性蓝）为主色，不引入新色。
- 字体：沿用 shell 字体栈（system-ui），不引入外部字体库。
- 语义色映射（全页统一）：输入=品牌蓝、输出=成功绿、缓存=警告琥珀、错误=危险红、中性=灰阶。
- 间距：8pt 体系 —— 4/8/12/16px 档位；栅格对齐 12px。
- 圆角：卡片 12px、控件 8px、胶囊 999px。

## 2. 布局与栅格
| 项 | 方案 |
|---|---|
| 指标卡栅格 | 默认 **2×2**（内容区实测 ≤556px）；`@container (min-width:560px)` 才切 4 列；`@container (max-width:380px)` 切 1 列；另保留 `@media (max-width:420px)` 兜底（不支持容器查询的浏览器） |
| 容器查询 | `.udb-root` 声明 `container-type: inline-size`，纯 CSS 自适应，无 JS 监听 |
| 会话列表序号 | 拆出独立 `.udb-sess-idx` 定宽列（24px），所有行标题左缘对齐 |
| 模型分布 | 固定列宽保留（名称 132 / 数值 110 / 占比 52），仅加宽名称列 |

## 3. 色彩收敛
- 模型分布条：7 色相彩虹 → **品牌蓝同色系透明度阶梯**（100/78/58/40/26/16/10%），排名信息靠长度表达，颜色只做层级区分。
- 指标卡：总消耗卡使用品牌色微染背景 + 品牌色点（hero 强调）；输入/输出/缓存读取分别用蓝/绿/琥珀点，避免「总消耗」与「输入」同色。
- 图表色不变（输入=蓝、输出=绿、缓存=琥珀），与指标卡语义一致。
- 卡片标题加 3px 品牌色竖条（opacity .7）作为分区锚点，克制用色。

## 4. 字体层级（对比度 ≥ AA）
| 元素 | 字号/字重 | 颜色（原 → 新） |
|---|---|---|
| 页标题 | 16/600 | label-primary（不变） |
| 副标题 | 12 | **tertiary → secondary** |
| 指标值 | 22/600（按位数 15/18/22 自适应） | label-primary |
| 指标标签 | 12 | label-secondary（不变） |
| 指标副行 | 11 | **tertiary → secondary** |
| 卡标题 | 13/600 | label-primary（不变） |
| 卡备注/图例/页脚 | 11–12 | **tertiary → secondary**（图例、备注、页脚） |
| 图表坐标轴 | 10 | tertiary（装饰性文本，保留） |

**指标值防截断双保险**：① 栅格改 2×2 后卡内可用宽度 ≥200px；② 按 `value.length` 动态降字号（>9 位 → 18px，>12 位 → 15px），任何宽度下不截断，`title` 属性始终保留完整数值。

## 5. 间距 / 圆角 / 阴影
- 统一：根 gap 12、指标卡 gap 12、卡内边距 16、卡片头下边距 12、图例上边距 10。
- 圆角：卡片 12、按钮/分段 8、进度条轨道 999（细条化 16px→8px 高）。
- 阴影：仅分段控件选中态保留轻阴影；卡片不加重投影（保持扁平）。

## 6. 状态与动效
- 交互态：按钮/分段控件/链接补充 `:focus-visible` 品牌色焦点环、`:active` 按压态、分段控件 hover 态。
- 扫描徽标：`udb-pulse` 透明度脉冲（1.8s），空状态扫描中显示脉冲圆点。
- 空状态：虚线边框卡片 + 居中布局；错误态保留原结构。
- `prefers-reduced-motion: reduce` 下禁用全部动效/过渡。

## 7. 响应式验收目标
| 视口 | 目标 |
|---|---|
| ≥1024（内容 556px） | 2×2 指标卡无截断、图表满宽、分布条无横向溢出 |
| 768（内容 476px） | 同上 |
| ≤430（内容 ~382px） | 1 列指标卡、卡片头允许换行、无横向滚动 |

## 8. 不改动清单（合规）
- 数据轮询/API 参数、SVG 图表计算逻辑、i18n、`settings.section` 注册、宿主半、构建配置、依赖（零新增）。
